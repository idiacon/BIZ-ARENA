# Biz Arena v0.1 вЂ” Phase 3 kickoff (СЌС‚Р°Рї 3)

Р­С‚РѕС‚ РґРѕРєСѓРјРµРЅС‚ Р·Р°РїСѓСЃРєР°РµС‚ **СЌС‚Р°Рї 3** РёР· `docs/v0.1-stabilization-phase-3.md`: persistence/runtime hardening, recovery Рё СѓСЃС‚РѕР№С‡РёРІРѕСЃС‚СЊ LAN-СЃРµСЃСЃРёР№.

## 1) РџР»Р°РЅ СЂР°Р±РѕС‚ РЅР° СЌС‚Р°Рї 3

### РЁР°Рі A вЂ” Persistence hardening

| Р—РѕРЅР° | РџСЂРѕРІРµСЂРєР° | РЎС‚Р°С‚СѓСЃ | РџСЂРёРјРµС‡Р°РЅРёРµ |
|---|---|---|---|
| snapshot schema | required fields + version | PASS | schemaVersion validate/migrate added for snapshot load |
| atomic write | temp file -> rename | PASS | DB writes now use tmp + backup + rename flow |
| load guardrails | fallback on broken snapshot | PASS | backup DB recovery + previousSnapshot fallback |
| migration path | compatibility across schema versions | PASS | normalizeDb + migrateRoomSnapshot |

### РЁР°Рі B вЂ” Runtime recovery

| РЎС†РµРЅР°СЂРёР№ | РћР¶РёРґР°РµРјРѕРµ РїРѕРІРµРґРµРЅРёРµ | РЎС‚Р°С‚СѓСЃ |
|---|---|---|
| server restart between ticks | room state restores | PASS | covered by restart/load/resume test |
| load after restart | players and room stay aligned | PASS | covered by restart/load/resume test |
| recover after business-action issue | room loop keeps running | PASS | room lifecycle/regression pack keeps loop alive after mixed actions |
| periodic persist failure | error is logged and process stays alive | TODO | still needs injected fs failure test |

### РЁР°Рі C вЂ” Reconnect / refresh resilience

- [x] refresh keeps player context after reload;
- [ ] repeat join into the same room is safe;
- [x] stale client state resyncs against server summary;
- [x] host/client roles remain correct after reconnect;
- [x] lobby/match/results statuses stay aligned after refresh.

### РЁР°Рі D вЂ” Data validation & limits

| РљР°С‚РµРіРѕСЂРёСЏ | РњРёРЅРёРјР°Р»СЊРЅР°СЏ Р·Р°С‰РёС‚Р° | РЎС‚Р°С‚СѓСЃ |
|---|---|---|
| action payloads | schema checks + bounds | PASS | action handlers clamp numeric payloads and reject unknown actions |
| room settings | ranges + defaults | PASS | updateRoomSettings clamps limits and falls back to defaults |
| account updates | normalize + sanitize | PASS | account and avatar inputs normalized before persistence |
| language/runtime flags | whitelist values | TODO | no dedicated whitelist layer yet |

## 2) Risk register (СЌС‚Р°Рї 3)

| ID | Risk | Impact | Signal | Mitigation | Status |
|---|---|---|---|---|---|
| P3-R001 | save corruption | high | load throws / parse errors | atomic write + backup snapshot | mitigated |
| P3-R002 | reconnect desync | high | UI differs from server summary | reload/resume integration coverage + roomSummary assertions | mitigated |
| P3-R003 | schema drift | medium | old saves fail on load | versioned migrations | mitigated |

## 3) Definition of done (СЌС‚Р°Рї 3)

Р­С‚Р°Рї 3 СЃС‡РёС‚Р°РµС‚СЃСЏ Р·Р°РєСЂС‹С‚С‹Рј, РєРѕРіРґР°:

1. persistence path СѓСЃС‚РѕР№С‡РёРІ Рє С‡Р°СЃС‚РёС‡РЅС‹Рј СЃР±РѕСЏРј Р·Р°РїРёСЃРё/С‡С‚РµРЅРёСЏ;
2. reconnect/refresh СЃС†РµРЅР°СЂРёРё РЅРµ Р»РѕРјР°СЋС‚ room/player state;
3. РµСЃС‚СЊ Р±Р°Р·РѕРІР°СЏ РІРµСЂСЃРёСЏ СЃС…РµРјС‹ Рё РїСЂРѕРІРµСЂРєР° РІС…РѕРґРЅС‹С… payload;
4. СЃРѕР±СЂР°РЅ РїРѕРґС‚РІРµСЂР¶РґС‘РЅРЅС‹Р№ backlog release-readiness РґР»СЏ СЌС‚Р°РїР° 4.

## 4) РљР°РЅРґРёРґР°С‚С‹ РІ РїРµСЂРІСѓСЋ СЂРµР°Р»РёР·Р°С†РёСЋ

- [x] Add `schemaVersion` and validate/migrate layer for snapshot load.
- [x] Move persist to atomic write (tmp + rename).
- [x] Add integration test: restart + load + resume room loop.
- [x] Add test for reconnect with player-scoped state validation.

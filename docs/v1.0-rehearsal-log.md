# Biz Arena v1.0 Rehearsal Log

## 2026-06-29 - Cloud SQLite 30-Student Load Gate

Command:

```powershell
npm run load:classroom
```

Environment:

- profile: `cloud-sqlite`
- students: `30`
- websocket connections: `30`
- server bind: `127.0.0.1`
- auto-selected load port: `33217`
- limits: `join p95 <= 1500 ms`, `state p95 <= 750 ms`, `action p95 <= 1000 ms`

Result:

| Run | Joined | Ready | WS | Join p95 | State p95 | Action p95 | Failures |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 1 | 30 | 30 | 30 | 919 ms | 42 ms | 38 ms | none |
| 2 | 30 | 30 | 30 | 960 ms | 54 ms | 50 ms | none |
| 3 | 30 | 30 | 30 | 952 ms | 49 ms | 47 ms | none |

Notes:

- The rehearsal script now chooses a free localhost port instead of assuming `3217`.
- The server is started with `BIZ_ARENA_HOST=127.0.0.1` during the load gate, which avoids collisions with other desktop apps listening on localhost.
- Each run verifies student thin state, unchanged-state shortcut, teacher overview counts, room start, and WebSocket connectivity.

## 2026-06-29 - Browser QA and Release Rehearsal After Mobile Teacher Fix

Commands:

```powershell
node scripts\capture-classroom-flow-screenshots.js
npm run e2e:classroom
npm run check
npm test
npm run smoke:classroom
npm run smoke:cloud
npm run smoke:cloud:sqlite
npm run smoke:vps
npm audit --omit=dev
npm run dist:classroom
npm run release:classroom-package
npm run release:cloud-package
npm run release:kai-server-package
npm run smoke:packaged
npm run load:classroom
npm run load:classroom
npm run load:classroom
```

Browser QA:

- `e2e:classroom`: teacher lobby, student lobby, teacher cockpit desktop/mobile, student first turn desktop/mobile, reconnect, Lite Mode, and browser errors check passed.
- Screenshot QA: `5/5` layout audits passed in `tmp/screenshots-v17-teacher-admin-polish/audit.json`.
- Mobile overflow fixes covered the teacher dashboard drawer and class route map inside the in-game teacher cockpit.

30-student rehearsal result:

| Run | Joined | Ready | WS | Join p95 | State p95 | Action p95 | Failures |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 1 | 30 | 30 | 30 | 945 ms | 41 ms | 43 ms | none |
| 2 | 30 | 30 | 30 | 958 ms | 42 ms | 36 ms | none |
| 3 | 30 | 30 | 30 | 985 ms | 42 ms | 43 ms | none |

Release artifacts:

- Fresh Server and Client Electron builds passed `smoke:packaged`.
- Fresh Classroom, Cloud, VPS, and KAI-server packages were copied to `D:\projects\тест бета\BizArena-0.9.0-beta.1`.
- Updated screenshots were copied to `D:\projects\тест бета\screenshots-v17-teacher-admin-polish`.

## 2026-06-30 - UI Role Contract Gate

Scope:

- Added `docs/ui-role-contract-v1.md` as the current Server/Admin, Teacher In-Game, and Student visibility contract.
- Added `GAME_TAB_ROLE_CONTRACT` in `public/app.js` so the in-game role tabs are defined in one place.
- Marked the primary game sidebar with `data-navigation-surface="game-sidebar"`.
- Marked the old hidden tab strip with `data-navigation-surface="legacy-tab-strip"` and `aria-hidden="true"` so it can be removed or kept deliberately in a later cleanup pass.
- Strengthened static and browser checks so student runtime cannot accidentally expose teacher navigation.

Commands:

```powershell
node --check public\app.js
node --check scripts\e2e-classroom-flows.js
npm test -- --test-name-pattern "UI contract"
npm run e2e:classroom
npm run check
npm test
graphify update .
```

Result:

- Static UI contract tests passed.
- Browser E2E passed with teacher visible sidebar tabs `teacher, overview, operations, purchase, competitors, market, events`.
- Browser E2E passed with student visible sidebar tabs `overview, operations, purchase, competitors, market, events`.
- Browser E2E confirmed the legacy tab strip is not visible.
- No browser errors, no readable-text failures, and no horizontal overflow failures in the covered classroom flow.

## 2026-06-30 - Teacher Lobby Preflight Contract Gate

Scope:

- Added stable teacher lobby preflight keys: `session`, `student-link`, `students`, `readiness`, `settings`.
- Added test selectors `data-preflight-ready`, `data-preflight-next`, `data-preflight-step`, `data-preflight-done`, and `data-preflight-action="start-match"`.
- Updated `docs/ui-role-contract-v1.md` with the teacher lobby preflight contract.
- Strengthened browser E2E so teacher lobby must expose the ready preflight gate while student lobby must not expose teacher preflight/start controls.

Commands:

```powershell
node --check public\app.js
node --check scripts\e2e-classroom-flows.js
npm test -- --test-name-pattern "UI contract"
npm run e2e:classroom
npm run check
npm test
```

Result:

- Static UI contract tests passed.
- Browser E2E passed with preflight keys `session, student-link, students, readiness, settings`.
- Browser E2E confirmed teacher `data-preflight-ready="true"` and `data-preflight-next="ready"` before launch.
- Browser E2E confirmed student lobby has no `[data-preflight-action]` and hides the teacher settings panel.
- Full `npm run check` and `npm test` passed after the preflight contract change.

## 2026-06-30 - Server Start Gate Authority

Scope:

- Added `roomStartGate(room)` as the server authority for match launch readiness.
- Local classroom start now requires two ready class players: the local host plus at least one student team.
- Cloud teacher rooms still start with one ready student because the teacher host is not a player company.
- Demo/tutorial flows now create rooms with `practiceMode: "demo"` or `practiceMode: "tutorial"` so they can intentionally start solo.
- `teacherControls.actions.startGame` now uses `roomStartGate(room).canStart`.
- `smoke:classroom` now proves solo classroom start is rejected before joining and readying a student.

Commands:

```powershell
node --check server.js
node --check public\app.js
node --check scripts\smoke-classroom-lan.js
node test\stabilization.test.js --test-name-pattern "server authority|cloud classroom|UI contract"
npm run e2e:classroom
npm run smoke:cloud
npm run smoke:classroom
npm run check
npm test
```

Result:

- Server rejects host-only local classroom start with HTTP 400.
- Server allows tutorial practice room to start with one ready player.
- Cloud teacher-owned room still starts with one ready student and keeps teacher host hidden from student summaries.
- Classroom browser E2E passed after the start-gate change.
- Cloud smoke passed after the start-gate change.
- Classroom smoke passed and reported `soloClassroomStartRejected: true`, `studentJoinAccepted: true`, and `serverAdminActionAccepted: true`.
- Full `npm run check` and `npm test` passed.

## 2026-06-30 - Teacher Lobby Start Gate UX

Scope:

- Added `teacherStartGateInfo(room)` in `public/app.js` so the teacher UI reads `teacherControls.startGate` as the single source of truth.
- Teacher lobby now exposes `.teacher-lobby-start-gate` with `data-start-gate-reason` and `data-start-gate-can-start`.
- Teacher lobby preflight, "what to do now", class readiness status, and primary host action now use the server start gate instead of local-only `humanCount/allReady` checks.
- Browser E2E now proves the host-only local classroom start button is disabled with `data-start-gate-reason="students"` before a student joins, then switches to `data-start-gate-reason="ready"` after student join and readiness.

Commands:

```powershell
node --check public\app.js
node --check scripts\e2e-classroom-flows.js
node test\stabilization.test.js --test-name-pattern "UI contract|server authority"
npm run e2e:classroom
npm run smoke:classroom
npm run check
npm test
npm run smoke:cloud
```

Result:

- Static UI contract and server authority checks passed.
- Browser E2E passed with blocked teacher lobby gate before student join and ready gate after join/readiness.
- Classroom smoke passed and kept `soloClassroomStartRejected: true`, `studentJoinAccepted: true`, and `serverAdminActionAccepted: true`.
- Cloud smoke passed with teacher auth, thin state, websocket invalidation, teacher overview, and cloud Crisis Cards intact.
- Full `npm run check` and `npm test` passed.

## 2026-06-30 - Student State v2 and First-Turn Contract

Scope:

- Added `student-state-v2` to `/api/state?view=student` and `/api/rooms/join`.
- Added `student-player-v2` through `playerStateSummary(room, player, viewerId, { view: "student" })`.
- Student room summary remains `student-v2`; student player summary now strips heavy strategy/admin branches (`executionPlan`, `intel`, `focusPlan`, `pivotPreview`, `operatingPlanPreview`, `researchEffects`).
- Active-game student player payload withholds `playerDebrief` until results and limits personnel hint candidates to the top 3.
- First-turn UI now exposes `data-student-flow-contract="first-turn-v2"`, `data-student-primary-step`, `data-student-route-ready`, and five `[data-student-route-step]` nodes.

Commands:

```powershell
node --check server.js
node --check server\http\routes.js
node --check scripts\smoke-cloud-classroom.js
node --check scripts\smoke-classroom-lan.js
node --check scripts\load-classroom.js
node test\server-refactor.test.js
node test\stabilization.test.js --test-name-pattern "cloud classroom|server authority|UI contract"
npm run smoke:cloud
npm run smoke:classroom
npm run check
npm test
npm run e2e:classroom
npm run load:classroom
```

Load result:

| Students | WS | Join p95 | State p95 | Action p95 | Failures |
| ---: | ---: | ---: | ---: | ---: | --- |
| 30 | 30 | 833 ms | 28 ms | 30 ms | none |

Result:

- Student API contracts passed in unit, cloud smoke, classroom smoke, and load gate.
- Browser E2E passed with student route contract selectors, student reconnect, Lite Mode, no browser errors, and no horizontal overflow.
- Full `npm run check` and `npm test` passed.

## 2026-06-30 - Teacher/Admin Match Lifecycle Contract

Scope:

- Added authoritative `teacher-lifecycle-v1` to teacher/full room summaries and immediate teacher/admin action responses.
- Unified local server admin and cloud teacher lifecycle actions through the same room action handler.
- Server now rejects `pause-game` outside `running` and `resume-game` outside `paused`.
- Paused matches remain accessible as active match screens after teacher reconnect while student business actions remain locked.
- Teacher cockpit exposes stable lifecycle selectors for phase, primary action, and expected next phase.
- Teacher results expose `class-debrief-v1`; student results expose `student-results-v1`.
- Removed the teacher classroom report pack and class discussion method from the student results surface.

Commands:

```powershell
npm run check
node test\server-refactor.test.js
node test\stabilization.test.js
npm run e2e:classroom
npm run smoke:classroom
npm run smoke:cloud
npm run smoke:cloud:sqlite
npm test
npm audit --omit=dev
npm run load:classroom
npm run smoke:vps
```

Result:

- Unit and stabilization lifecycle tests passed.
- Browser E2E passed `start -> pause -> teacher reconnect -> resume -> next turn -> finish -> debrief`.
- Browser E2E confirmed the teacher receives the class debrief and the student receives only personal results.
- LAN smoke passed lifecycle, debrief, and student role-boundary checks.
- Cloud JSON and SQLite smokes passed lifecycle, reconnect overview, debrief, and student role-boundary checks.
- Full test suite passed and production dependency audit reported `0 vulnerabilities`.
- VPS/KAI SQLite profile restored the room and student session after restart, kept registration locked, and delivered WebSocket invalidation.
- The 30-student rehearsal completed with 30 WebSocket connections, no failures, `state p95 35 ms`, and `action p95 37 ms`.

## 2026-06-30 - Single Game Navigation Contract

Scope:

- Removed the hidden `legacy-tab-strip` and its duplicated teacher/student tab buttons from `public/index.html`.
- Removed the unused `advanced-tabs` wrapper and its client synchronization branch.
- Removed dead `.game-tabs`, `.tab-button`, `.advanced-tabs`, and `.control-strip` CSS.
- Kept `data-navigation-surface="game-sidebar"` as the only static game navigation surface.
- Preserved role ownership: teacher sees the cockpit tab, student does not.

Acceptance:

- Static tests require the legacy navigation surface to be absent from the DOM and CSS.
- Browser E2E requires the legacy selector to be absent for both teacher and student sessions.
- Existing role-tab visibility and active-tab behavior remain covered by the same regression suite.

Verification:

```powershell
npm run check
npm test
npm run e2e:classroom
npm run smoke:classroom
npm run load:classroom
```

Result:

- Teacher and student browser sessions passed with no browser errors.
- LAN smoke passed the lifecycle and role boundaries after the DOM cleanup.
- The post-cleanup 30-student run completed with 30 WebSocket sessions, `state p95 50 ms`, and `action p95 40 ms`.

## 2026-06-30 - Automated Three-Run Classroom Rehearsal Gate

Scope:

- Added `npm run rehearsal:classroom`.
- The runner performs at least three sequential, isolated `cloud + sqlite` classroom load runs.
- Every run requires 30 joined students, 30 ready students, 30 WebSocket sessions, a started room, and the existing p95 limits.
- The aggregate contract is `classroom-rehearsal-v1`.
- Timestamped and latest JSON reports are written to `.runtime/rehearsals/`.

Command:

```powershell
npm run rehearsal:classroom
```

Aggregate result:

| Runs | Students per run | WS per run | Worst join p95 | Worst state p95 | Worst action p95 | Failures |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 3 | 30 | 30 | 850 ms | 47 ms | 37 ms | none |

The v1.0 three-rehearsal performance gate is now reproducible with one command instead of relying on manually copied one-off results.

## 2026-06-30 - Post-Hardening Beta Rebuild

Commands:

```powershell
npm run dist:classroom
npm run smoke:packaged
npm run release:classroom-package
npm run release:cloud-package
npm run release:kai-server-package
```

Result:

- Rebuilt Server and Client setup/portable executables from the lifecycle and single-navigation source.
- Packaged smoke launched both portable executables and verified SQLite server health at `/api/health`.
- Rebuilt Classroom, Cloud, VPS, and KAI server ZIP packages plus their release manifests.
- Synchronized current artifacts, teacher handoffs, UI contract, and rehearsal evidence to `тест бета/BizArena-0.9.0-beta.1`.

## 2026-07-01 - SQLite Recovery And Fail-Closed Gate

Scope:

- SQLite now preserves the previous valid runtime payload before replacing `main`.
- A corrupt `main` payload is repaired automatically from `previous` and reported as `storage-recovered`.
- Startup with no valid runtime payload exits before `listen()` and reports `storage-load-failed`; it no longer falls back to an empty classroom database.
- KAI/VPS runbooks distinguish local payload recovery from daily SQLite backup/restore.

Commands:

```powershell
npm run check
npm test
npm run smoke:cloud:sqlite
npm run smoke:vps
npm run rehearsal:classroom
```

Aggregate rehearsal result after the storage change:

| Runs | Students per run | WS per run | Worst join p95 | Worst state p95 | Worst action p95 | Failures |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 3 | 30 | 30 | 870 ms | 51 ms | 41 ms | none |

The VPS smoke also restored the room and student session after a real server restart with durable SQLite storage and registration locked.

## 2026-07-01 - Explicit Schema Migration And Beta Rebuild

Scope:

- Database and room snapshots now migrate through explicit `v1 -> v2 -> v3` registries.
- Missing versions are treated as the supported legacy `v1` contract; retained schema `v2` is based on commit `151ba51`.
- Malformed and future schemas fail closed with stable error codes.
- SQLite may recover an unsupported future `main` payload from a valid `previous` state.
- A child-process regression test proves an existing SQLite database loads during module initialization.

Verification:

```powershell
npm run check
npm test
npm run smoke:classroom
npm run smoke:cloud:sqlite
npm run smoke:vps
npm run e2e:classroom
npm run rehearsal:classroom
npm audit --omit=dev
npm run dist:classroom
npm run smoke:packaged
npm run release:classroom-package
npm run release:cloud-package
npm run release:vps-package
npm run release:kai-server-package
```

Result:

- Full automated suite passed after adding migration and startup regressions.
- Classroom, cloud SQLite, VPS restart, browser E2E, and packaged Electron checks passed.
- Rehearsal completed 3 runs with 30 students and 30 WebSockets per run; worst p95 was `join 886 ms`, `state 30 ms`, `action 23 ms` with no failures.
- Production dependency audit reported `0 vulnerabilities`.
- Server/Client setup and portable executables plus Classroom/Cloud/VPS/KAI packages were rebuilt from the current source.

## 2026-07-11 - Pilot Beta GitHub Checkpoint

Scope:

- Rebuilt the Server and Client Electron applications with the native SQLite module compiled for the Electron ABI before packaging.
- Verified the packaged portable applications against the SQLite health endpoint.
- Finalized the public README, active LAN/VPS positioning, CI workflow, and role-based Full/Standard/Lite product contract.
- Migrated the local Codex/OMX routing profile to `5.6 Sol` for leadership and review, `5.6 Terra` for implementation, and `5.6 Luna` for fast exploration.

Verification:

```powershell
npm run check
npm test
npm run smoke:classroom
npm run smoke:cloud
npm run smoke:cloud:sqlite
npm run smoke:vps
npm run dist:classroom
npm run smoke:packaged-electron
```

Result:

- All `208` automated tests passed.
- Local Classroom, cloud JSON, cloud SQLite, and durable VPS restart smoke tests passed.
- Fresh Server and Client portable executables started successfully and reported healthy SQLite storage.
- `graphify update .` completed with `2155` nodes and `3664` edges.
- The final `npm audit --omit=dev` request could not reach the npm registry because the local network path timed out/reset; this checkpoint does not treat the unavailable advisory response as a successful audit.
